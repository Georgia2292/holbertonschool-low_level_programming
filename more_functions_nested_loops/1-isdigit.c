#include "main.h"

/**
 * _isdigit - Check if character c is a digit from 1 to 9
 * @c: char to check
 * Return: 0 or 1
 */

int _isdigit(int c)

{
	if (c >= 0 && c <= 9)
	{
	return (1);
	}
	return (0);
}
